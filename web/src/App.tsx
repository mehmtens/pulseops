import { FormEvent, useCallback, useEffect, useState } from 'react'

type Monitor = {
  id: number; name: string; url: string; intervalSeconds: number; timeoutSeconds: number; active: boolean; public: boolean
  lastCheckedAt: string | null; lastStatusCode: number | null; lastResponseMs: number | null; lastError: string | null
  certificateExpiresAt: string | null; uptime24h: number; incidentStartedAt: string | null
  failureThreshold: number; recoveryThreshold: number; consecutiveFailures: number; consecutiveSuccesses: number; maintenanceUntil: string | null
  maintenanceActive: boolean
  monitorType: 'http' | 'heartbeat' | 'tcp' | 'dns'; expectedKeyword: string
  notificationChannels: ('email' | 'webhook' | 'push')[]; escalationDelaySeconds: number; statusComponent: string
}
type MaintenanceSchedule = { id: number; monitorId: number; weekday: number; startMinute: number; durationMinutes: number; timezone: string; exceptionDates: string[]; enabled: boolean }
type MonitorForm = { name: string; monitorType: 'http' | 'heartbeat' | 'tcp' | 'dns'; url: string; expectedKeyword: string; intervalSeconds: number; timeoutSeconds: number; failureThreshold: number; recoveryThreshold: number; maintenanceUntil: string; active: boolean; public: boolean; notificationChannels: ('email' | 'webhook' | 'push')[]; escalationDelaySeconds: number; statusComponent: string }
type IncidentUpdate = { id: number; status: 'investigating' | 'identified' | 'monitoring' | 'resolved'; message: string; createdAt: string }
type Incident = { id: number; monitorName: string; startedAt: string; resolvedAt: string | null; cause: string; acknowledgedAt?: string | null; note?: string; updates?: IncidentUpdate[] }
type ApiKey = { id: number; name: string; prefix: string; scope: 'read' | 'write' | 'admin'; lastUsedAt: string | null; createdAt: string }
type Invitation = { id: number; name: string; prefix: string; scope: 'read' | 'write' | 'admin'; expiresAt: string; acceptedAt: string | null; revokedAt: string | null; createdAt: string }
type Report = { days: number; monitors: { monitorId: number; monitorName: string; uptime: number; averageResponseMs: number; checks: number }[] }
type AuditEvent = { actor: string; action: string; resource: string; status: number; createdAt: string }
type NotificationMetrics = { total: number; delivered: number; pending: number; retrying: number; oldestPendingAt: string | null; channels: Record<string, { total: number; delivered: number }> }
type WorkerStatus = { workers: { region: string; startedAt: string; lastSeenAt: string; lastClaimedAt: string | null; lastResultAt: string | null; claims: number; results: number; online: boolean }[]; onlineRegions: number; requiredRegions: number; offlineAfterSeconds: number; warnings: string[] }
const blank: MonitorForm = { name: '', monitorType: 'http', url: '', expectedKeyword: '', intervalSeconds: 60, timeoutSeconds: 10, failureThreshold: 2, recoveryThreshold: 2, maintenanceUntil: '', active: true, public: true, notificationChannels: ['email', 'webhook', 'push'], escalationDelaySeconds: 0, statusComponent: 'Services' }

function stateOf(monitor: Monitor) {
  if (!monitor.active) return 'paused'
  if (monitor.maintenanceActive || monitor.maintenanceUntil && new Date(monitor.maintenanceUntil) > new Date()) return 'maintenance'
  if (!monitor.lastCheckedAt) return 'pending'
  if (monitor.incidentStartedAt) return 'down'
  return monitor.consecutiveFailures > 0 ? 'degraded' : 'up'
}

function date(value: string | null) {
  return value ? new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value)) : 'Not checked yet'
}

function InviteAcceptance({ invitation }: { invitation: string }) {
  const [state, setState] = useState<'ready' | 'busy' | 'done' | 'error'>('ready')
  const [message, setMessage] = useState('')
  async function accept() {
    setState('busy')
    const response = await fetch('/api/invitations/accept', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ token: invitation }) })
    const data = await response.json().catch(() => ({}))
    if (!response.ok) { setMessage(data.error || 'Invitation could not be accepted.'); setState('error'); return }
    sessionStorage.setItem('pulseops-token', data.token); history.replaceState({}, '', '/'); setState('done'); setMessage(`Welcome, ${data.name}. Your independent access is ready.`)
  }
  return <div className="login"><div className="login-card"><a className="brand" href="/">Pulse<span>Ops</span></a><p className="kicker">TEAM INVITATION</p><h1>Join the control room.</h1><p>{message || 'Accept this one-time invitation to create your revocable member access.'}</p>{state === 'done' ? <button onClick={() => location.reload()}>Open dashboard</button> : <button disabled={state === 'busy'} onClick={accept}>{state === 'busy' ? 'Accepting…' : 'Accept invitation'}</button>}{state === 'error' && <p className="error" role="alert">Ask an admin for a fresh invitation.</p>}</div></div>
}

function PublicStatus() {
  const [monitors, setMonitors] = useState<Monitor[]>([])
  const [incidents, setIncidents] = useState<Incident[]>([])
  const [updatedAt, setUpdatedAt] = useState('')
  const [page, setPage] = useState({ name: 'PulseOps', message: 'Live service health and incident updates.' })
  const load = useCallback(async () => {
    const response = await fetch('/api/status')
    if (!response.ok) return
    const data = await response.json()
    setMonitors(data.monitors); setIncidents(data.incidents || []); setUpdatedAt(data.updatedAt); if (data.page) setPage(data.page)
  }, [])
  useEffect(() => { load(); const events = new EventSource('/api/events'); events.addEventListener('status', load); const timer = window.setInterval(load, 60_000); return () => { events.close(); window.clearInterval(timer) } }, [load])
  const operational = monitors.every((monitor) => stateOf(monitor) !== 'down')
  const components = monitors.reduce<Record<string, Monitor[]>>((groups, monitor) => { (groups[monitor.statusComponent || 'Services'] ||= []).push(monitor); return groups }, {})
  return <div className="shell status-page">
    <header><a className="brand" href="/">{page.name}</a><span className="live"><i /> Live</span></header>
    <main>
      <section className={`hero-status ${operational ? 'healthy' : 'outage'}`}><div className="big-dot" /><div><p className="kicker">CURRENT STATUS</p><h1>{operational ? 'All systems operational' : 'Service disruption detected'}</h1><p>{page.message} {updatedAt ? `Updated ${date(updatedAt)}` : 'Loading current status…'}</p></div></section>
      <div className="status-list">{monitors.length === 0 ? <div className="empty">No public services configured.</div> : Object.entries(components).map(([component, items]) => <section className="status-component" key={component}><h2>{component}</h2>{items.map((monitor) => <article className="status-row" key={monitor.id}><div><h3>{monitor.name}</h3><p>{monitor.uptime24h.toFixed(2)}% uptime over 24 hours</p></div><span className={`pill ${stateOf(monitor)}`}>{stateOf(monitor)}</span></article>)}</section>)}</div>
      <section className="incident-section"><div className="section-title"><div><p className="kicker">HISTORY</p><h2>Recent incidents</h2></div></div><div className="timeline">{incidents.length === 0 ? <div className="empty">No incidents reported.</div> : incidents.map((incident) => <article className="timeline-row" key={incident.id}><i className={incident.resolvedAt ? 'resolved' : 'open'} /><div><div className="timeline-title"><strong>{incident.monitorName}</strong><span className={`pill ${incident.resolvedAt ? 'up' : 'down'}`}>{incident.resolvedAt ? 'resolved' : 'investigating'}</span></div><p>{incident.cause}</p>{incident.updates?.map((update) => <div className="public-update" key={update.id}><strong>{update.status}</strong><span>{update.message}</span><small>{date(update.createdAt)}</small></div>)}<small>{date(incident.startedAt)}{incident.resolvedAt && ` · Resolved ${date(incident.resolvedAt)}`}</small></div></article>)}</div></section>
    </main>
    <footer>{page.name} · Powered by PulseOps</footer>
  </div>
}

export function App() {
  if (location.pathname === '/status') return <PublicStatus />
  const invitation = new URLSearchParams(location.hash.slice(1)).get('invite')
  if (invitation) {
    history.replaceState(null, '', location.pathname)
    return <InviteAcceptance invitation={invitation} />
  }
  const [token, setToken] = useState(() => sessionStorage.getItem('pulseops-token') || '')
  const [draftToken, setDraftToken] = useState('')
  const [monitors, setMonitors] = useState<Monitor[]>([])
  const [incidents, setIncidents] = useState<Incident[]>([])
  const [keys, setKeys] = useState<ApiKey[]>([])
  const [invitations, setInvitations] = useState<Invitation[]>([])
  const [report, setReport] = useState<Report | null>(null)
  const [audit, setAudit] = useState<AuditEvent[]>([])
  const [notificationMetrics, setNotificationMetrics] = useState<NotificationMetrics | null>(null)
  const [workerStatus, setWorkerStatus] = useState<WorkerStatus | null>(null)
  const [schedules, setSchedules] = useState<Record<number, MaintenanceSchedule[]>>({})
  const [scheduleDraft, setScheduleDraft] = useState({ weekday: 1, startMinute: 0, durationMinutes: 60, timezone: Intl.DateTimeFormat().resolvedOptions().timeZone || 'UTC', exceptionDates: '' })
  const [newKey, setNewKey] = useState({ name: '', scope: 'read' as 'read' | 'write' | 'admin' })
  const [newInvitation, setNewInvitation] = useState({ name: '', scope: 'write' as 'read' | 'write' | 'admin', expiresInHours: 72 })
  const [createdToken, setCreatedToken] = useState('')
  const [invitationLink, setInvitationLink] = useState('')
  const [pushMonitorIds, setPushMonitorIds] = useState<number[]>([])
  const [heartbeatUrl, setHeartbeatUrl] = useState('')
  const [form, setForm] = useState<MonitorForm>(blank)
  const [editing, setEditing] = useState<number | null>(null)
  const [error, setError] = useState('')
  const [busy, setBusy] = useState(false)

  const request = useCallback(async (path: string, options: RequestInit = {}) => {
    const response = await fetch(path, { ...options, headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}`, ...options.headers } })
    if (response.status === 401) { sessionStorage.removeItem('pulseops-token'); setToken(''); throw new Error('Session expired') }
    if (!response.ok) { const data = await response.json().catch(() => ({})); throw new Error(data.error || 'Request failed') }
    return response.status === 204 ? null : response.json()
  }, [token])
  const refreshStatus = useCallback(async () => { if (!token) return; const [nextMonitors, nextIncidents, nextReport] = await Promise.all([request('/api/monitors'), request('/api/incidents'), request('/api/reports/uptime?days=30')]); setMonitors(nextMonitors); setIncidents(nextIncidents); setReport(nextReport) }, [request, token])
  const load = useCallback(async () => { if (!token) return; try { const [, nextSchedules] = await Promise.all([refreshStatus(), request('/api/maintenance-schedules')]); const grouped = (nextSchedules as MaintenanceSchedule[]).reduce<Record<number, MaintenanceSchedule[]>>((items, schedule) => { (items[schedule.monitorId] ||= []).push(schedule); return items }, {}); setSchedules(grouped); setError(''); try { setKeys(await request('/api/keys')); setInvitations(await request('/api/invitations')) } catch { setKeys([]); setInvitations([]) } try { setAudit(await request('/api/audit')) } catch { setAudit([]) } try { const status = await request('/api/workers'); setWorkerStatus(status && Array.isArray(status.workers) ? status : null) } catch { setWorkerStatus(null) } try { setNotificationMetrics(await request('/api/notifications/metrics')) } catch { setNotificationMetrics(null) } } catch (err) { setError((err as Error).message) } }, [refreshStatus, request, token])
  useEffect(() => { load() }, [load])
  useEffect(() => { if (!token) return; const events = new EventSource('/api/events'); const refresh = () => { refreshStatus().catch((err) => setError((err as Error).message)) }; events.addEventListener('status', refresh); const timer = window.setInterval(refresh, 60_000); return () => { events.close(); window.clearInterval(timer) } }, [refreshStatus, token])
  useEffect(() => { if (!token) return; const refreshWorkers = () => { request('/api/workers').then((status) => setWorkerStatus(status && Array.isArray(status.workers) ? status : null)).catch(() => setWorkerStatus(null)) }; const timer = window.setInterval(refreshWorkers, 10_000); return () => window.clearInterval(timer) }, [request, token])

  function login(event: FormEvent) { event.preventDefault(); const clean = draftToken.trim(); if (clean.length < 16) { setError('Token must be at least 16 characters.'); return }; sessionStorage.setItem('pulseops-token', clean); setToken(clean); setError('') }
  async function save(event: FormEvent) { event.preventDefault(); setBusy(true); setError(''); try { const payload = { ...form, maintenanceUntil: form.maintenanceUntil ? new Date(form.maintenanceUntil).toISOString() : null }; const result = await request(editing ? `/api/monitors/${editing}` : '/api/monitors', { method: editing ? 'PUT' : 'POST', body: JSON.stringify(payload) }); if (result?.heartbeatUrl) setHeartbeatUrl(`${location.origin}${result.heartbeatUrl}`); setForm(blank); setEditing(null); await load() } catch (err) { setError((err as Error).message) } finally { setBusy(false) } }
  function edit(monitor: Monitor) { setEditing(monitor.id); const maintenanceUntil = monitor.maintenanceUntil ? new Date(new Date(monitor.maintenanceUntil).getTime() - new Date().getTimezoneOffset() * 60000).toISOString().slice(0, 16) : ''; setForm({ name: monitor.name, monitorType: monitor.monitorType, url: monitor.url, expectedKeyword: monitor.expectedKeyword, intervalSeconds: monitor.intervalSeconds, timeoutSeconds: monitor.timeoutSeconds, failureThreshold: monitor.failureThreshold, recoveryThreshold: monitor.recoveryThreshold, maintenanceUntil, active: monitor.active, public: monitor.public, notificationChannels: monitor.notificationChannels, escalationDelaySeconds: monitor.escalationDelaySeconds, statusComponent: monitor.statusComponent }); scrollTo({ top: 0, behavior: 'smooth' }) }
  async function remove(monitor: Monitor) { if (!confirm(`Delete ${monitor.name} and its history?`)) return; try { await request(`/api/monitors/${monitor.id}`, { method: 'DELETE' }); await load() } catch (err) { setError((err as Error).message) } }
  async function annotate(incident: Incident) { const note = prompt('Incident note', incident.note || ''); if (note === null) return; try { await request(`/api/incidents/${incident.id}`, { method: 'PATCH', body: JSON.stringify({ acknowledged: true, note }) }); await load() } catch (err) { setError((err as Error).message) } }
  async function createKey(event: FormEvent) { event.preventDefault(); try { const result = await request('/api/keys', { method: 'POST', body: JSON.stringify(newKey) }); setCreatedToken(result.token); setNewKey({ name: '', scope: 'read' }); await load() } catch (err) { setError((err as Error).message) } }
  async function deleteKey(key: ApiKey) { if (!confirm(`Revoke ${key.name}?`)) return; try { await request(`/api/keys/${key.id}`, { method: 'DELETE' }); await load() } catch (err) { setError((err as Error).message) } }
  async function createInvitation(event: FormEvent) { event.preventDefault(); try { const result = await request('/api/invitations', { method: 'POST', body: JSON.stringify(newInvitation) }); setInvitationLink(`${location.origin}${result.path}`); setNewInvitation({ name: '', scope: 'write', expiresInHours: 72 }); await load() } catch (err) { setError((err as Error).message) } }
  async function revokeInvitation(invitation: Invitation) { try { await request(`/api/invitations/${invitation.id}`, { method: 'DELETE' }); await load() } catch (err) { setError((err as Error).message) } }
  async function publishUpdate(incident: Incident) { const message = prompt('Public incident update'); if (!message) return; const status = prompt('Status: investigating, identified, monitoring, or resolved', incident.resolvedAt ? 'resolved' : 'investigating'); if (!status) return; try { await request(`/api/incidents/${incident.id}/updates`, { method: 'POST', body: JSON.stringify({ status, message }) }); await load() } catch (err) { setError((err as Error).message) } }
  function vapidKey(value: string) { const padding = '='.repeat((4 - value.length % 4) % 4); const raw = atob((value + padding).replace(/-/g, '+').replace(/_/g, '/')); return Uint8Array.from([...raw].map((char) => char.charCodeAt(0))) }
  async function configurePush() { try { if (!('serviceWorker' in navigator) || !('PushManager' in window)) throw new Error('Web Push is not supported by this browser'); const permission = await Notification.requestPermission(); if (permission !== 'granted') throw new Error('Notification permission was not granted'); const { publicKey } = await request('/api/push/vapid-public-key'); const registration = await navigator.serviceWorker.ready; let subscription = await registration.pushManager.getSubscription(); if (!subscription) subscription = await registration.pushManager.subscribe({ userVisibleOnly: true, applicationServerKey: vapidKey(publicKey) }); const json = subscription.toJSON(); await request('/api/push/subscription', { method: 'PUT', body: JSON.stringify({ endpoint: subscription.endpoint, keys: json.keys, monitorIds: pushMonitorIds }) }); setError(''); alert('Browser alerts updated.') } catch (err) { setError((err as Error).message) } }
  async function downloadReport() { try { const response = await fetch('/api/reports/uptime?days=30&format=csv', { headers: { Authorization: `Bearer ${token}` } }); if (!response.ok) throw new Error('Download failed'); const href=URL.createObjectURL(await response.blob()); const link=document.createElement('a'); link.href=href; link.download='pulseops-uptime.csv'; link.click(); URL.revokeObjectURL(href) } catch (err) { setError((err as Error).message) } }
  async function addSchedule(monitorId: number) { try { const payload = { ...scheduleDraft, exceptionDates: scheduleDraft.exceptionDates.split(',').map((date) => date.trim()).filter(Boolean) }; const result = await request(`/api/monitors/${monitorId}/maintenance-schedules`, { method: 'POST', body: JSON.stringify(payload) }); setSchedules((current) => ({ ...current, [monitorId]: [...(current[monitorId] || []), result] })); await refreshStatus() } catch (err) { setError((err as Error).message) } }
  async function removeSchedule(monitorId: number, scheduleId: number) { try { await request(`/api/monitors/${monitorId}/maintenance-schedules/${scheduleId}`, { method: 'DELETE' }); setSchedules((current) => ({ ...current, [monitorId]: (current[monitorId] || []).filter((schedule) => schedule.id !== scheduleId) })); await refreshStatus() } catch (err) { setError((err as Error).message) } }
  function scheduleTime(minutes: number) { return `${String(Math.floor(minutes / 60)).padStart(2, '0')}:${String(minutes % 60).padStart(2, '0')}` }
  const weekdays = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday']

  if (!token) return <div className="login"><div className="login-card"><a className="brand" href="/">Pulse<span>Ops</span></a><p className="kicker">CONTROL ROOM</p><h1>See trouble before your users do.</h1><p>Enter the API token configured on your PulseOps server.</p><form onSubmit={login}><label>API token<input type="password" value={draftToken} onChange={(e) => setDraftToken(e.target.value)} autoComplete="current-password" required /></label>{error && <p className="error" role="alert">{error}</p>}<button>Open dashboard</button></form><a className="public-link" href="/status">View public status page →</a></div></div>

  const down = monitors.filter((m) => stateOf(m) === 'down').length
  const average = monitors.length ? monitors.reduce((sum, m) => sum + m.uptime24h, 0) / monitors.length : 100
  return <div className="shell dashboard">
    <header><a className="brand" href="/">Pulse<span>Ops</span></a><nav><a href="/status">Public status</a><button className="text-button" onClick={() => { sessionStorage.removeItem('pulseops-token'); setToken('') }}>Sign out</button></nav></header>
    <main>
      <div className="headline"><div><p className="kicker">CONTROL ROOM</p><h1>Good {new Date().getHours() < 12 ? 'morning' : 'evening'}.</h1><p>Everything that matters, in one quiet place.</p></div><div className={`overview ${down ? 'outage' : ''}`}><i />{down ? `${down} incident${down > 1 ? 's' : ''}` : 'All clear'}</div></div>
      <div className="metrics"><div><strong>{monitors.length}</strong><span>Monitors</span></div><div><strong>{average.toFixed(2)}%</strong><span>24h uptime</span></div><div><strong>{down}</strong><span>Open incidents</span></div></div>
      {workerStatus && <section className="panel workers"><div className="section-title"><div><p className="kicker">REGIONAL FLEET</p><h2>Worker availability</h2></div><span className={`delivery-state ${workerStatus.warnings.length ? 'attention' : ''}`}>{workerStatus.onlineRegions}/{workerStatus.requiredRegions} regions ready</span></div>{workerStatus.warnings.map((warning) => <p className="worker-warning" role="alert" key={warning}>{warning}</p>)}<div className="worker-grid">{workerStatus.workers.length === 0 ? <div className="empty">No regional worker has checked in.</div> : workerStatus.workers.map((worker) => <div className="worker-row" key={worker.region}><span className={`pill ${worker.online ? 'up' : 'down'}`}>{worker.online ? 'online' : 'offline'}</span><strong>{worker.region}</strong><span>Seen {date(worker.lastSeenAt)}</span><span>{worker.results} results / {worker.claims} claims</span></div>)}</div><p className="delivery-note">A worker is marked offline after {workerStatus.offlineAfterSeconds} seconds without a claim or result.</p></section>}
      {report && <section className="panel report"><div className="section-title"><div><p className="kicker">30 DAY REPORT</p><h2>Service reliability</h2></div><button className="secondary" onClick={downloadReport}>Download CSV</button></div><div className="report-grid">{report.monitors.map((item) => <div key={item.monitorId}><strong>{item.uptime.toFixed(2)}%</strong><span>{item.monitorName} · {item.averageResponseMs} ms · {item.checks} checks</span></div>)}</div></section>}
      {notificationMetrics && <section className="panel delivery"><div className="section-title"><div><p className="kicker">ALERT DELIVERY</p><h2>Notification queue</h2></div><span className={`delivery-state ${notificationMetrics.retrying ? 'attention' : ''}`}>{notificationMetrics.retrying ? 'Needs attention' : 'Healthy'}</span></div><div className="delivery-grid"><div><strong>{notificationMetrics.delivered}</strong><span>Delivered</span></div><div><strong>{notificationMetrics.pending}</strong><span>Waiting</span></div><div><strong>{notificationMetrics.retrying}</strong><span>Retrying</span></div><div><strong>{notificationMetrics.total}</strong><span>Total events</span></div></div>{notificationMetrics.oldestPendingAt && <p className="delivery-note">Oldest pending attempt: {date(notificationMetrics.oldestPendingAt)}</p>}</section>}
      <section className="panel form-panel"><div><p className="kicker">{editing ? 'EDIT MONITOR' : 'NEW MONITOR'}</p><h2>{editing ? 'Update endpoint' : 'Watch an endpoint'}</h2></div><form onSubmit={save}>
        <label>Name<input value={form.name} maxLength={100} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Marketing site" required /></label>
        <label>Monitor type<select value={form.monitorType} disabled={editing !== null} onChange={(e) => setForm({ ...form, monitorType: e.target.value as MonitorForm['monitorType'] })}><option value="http">HTTP / HTTPS</option><option value="heartbeat">Cron heartbeat</option><option value="tcp">TCP port</option><option value="dns">DNS lookup</option></select></label>
        {form.monitorType === 'http' && <><label className="wide">HTTP/HTTPS URL<input type="url" value={form.url} maxLength={2048} onChange={(e) => setForm({ ...form, url: e.target.value })} placeholder="https://example.com/health" required /></label><label className="wide">Expected content <span className="optional">optional</span><input value={form.expectedKeyword} maxLength={500} onChange={(e) => setForm({ ...form, expectedKeyword: e.target.value })} placeholder="ok" /></label></>}
        {(form.monitorType === 'tcp' || form.monitorType === 'dns') && <label className="wide">{form.monitorType === 'tcp' ? 'Host and port' : 'Hostname'}<input value={form.url} maxLength={2048} onChange={(e) => setForm({ ...form, url: e.target.value })} placeholder={form.monitorType === 'tcp' ? 'example.com:443' : 'example.com'} required /></label>}
        <label>Interval (seconds)<input type="number" min={15} max={86400} value={form.intervalSeconds} onChange={(e) => setForm({ ...form, intervalSeconds: Number(e.target.value) })} required /></label>
        <label>Timeout (seconds)<input type="number" min={1} max={30} value={form.timeoutSeconds} onChange={(e) => setForm({ ...form, timeoutSeconds: Number(e.target.value) })} required /></label>
        <label>Failures before incident<input type="number" min={1} max={10} value={form.failureThreshold} onChange={(e) => setForm({ ...form, failureThreshold: Number(e.target.value) })} required /></label>
        <label>Successes before recovery<input type="number" min={1} max={10} value={form.recoveryThreshold} onChange={(e) => setForm({ ...form, recoveryThreshold: Number(e.target.value) })} required /></label>
        <label>Status-page component<input value={form.statusComponent} maxLength={100} onChange={(e) => setForm({ ...form, statusComponent: e.target.value })} required /></label>
        <label>Escalation delay (seconds)<input type="number" min={0} max={86400} value={form.escalationDelaySeconds} onChange={(e) => setForm({ ...form, escalationDelaySeconds: Number(e.target.value) })} required /></label>
        <fieldset className="wide channel-picker"><legend>Notification channels</legend>{(['email', 'webhook', 'push'] as const).map((channel) => <label className="check" key={channel}><input type="checkbox" checked={form.notificationChannels.includes(channel)} onChange={(e) => setForm({ ...form, notificationChannels: e.target.checked ? [...form.notificationChannels, channel] : form.notificationChannels.filter((item) => item !== channel) })} /> {channel === 'push' ? 'Web Push' : channel[0].toUpperCase() + channel.slice(1)}</label>)}</fieldset>
        <label className="wide">Maintenance until <span className="optional">optional</span><input type="datetime-local" value={form.maintenanceUntil} onChange={(e) => setForm({ ...form, maintenanceUntil: e.target.value })} /></label>
        <label className="check"><input type="checkbox" checked={form.active} onChange={(e) => setForm({ ...form, active: e.target.checked })} /> Active</label><label className="check"><input type="checkbox" checked={form.public} onChange={(e) => setForm({ ...form, public: e.target.checked })} /> Public</label>
        <div className="actions"><button disabled={busy}>{busy ? 'Saving…' : editing ? 'Save changes' : 'Add monitor'}</button>{editing && <button type="button" className="secondary" onClick={() => { setEditing(null); setForm(blank) }}>Cancel</button>}</div>
      </form></section>
      {heartbeatUrl && <p className="token-result"><strong>Send a POST after every successful job — copy now:</strong> <code>{heartbeatUrl}</code></p>}
      {error && <p className="error banner" role="alert">{error}</p>}
      <section className="monitor-section"><div className="section-title"><div><p className="kicker">ENDPOINTS</p><h2>Your monitors</h2></div><button className="secondary" onClick={load}>Refresh</button></div>
        <div className="monitor-grid">{monitors.length === 0 ? <div className="empty">Add your first endpoint above. PulseOps will check it within seconds.</div> : monitors.map((monitor) => <article className="monitor-card" key={monitor.id}><div className="card-top"><span className={`pill ${stateOf(monitor)}`}>{stateOf(monitor)}</span><span className="uptime">{monitor.uptime24h.toFixed(2)}%</span></div><h3>{monitor.name}</h3>{monitor.monitorType === 'http' ? <a href={monitor.url} target="_blank" rel="noreferrer">{monitor.url}</a> : <span className="monitor-kind">{monitor.monitorType === 'heartbeat' ? `Cron heartbeat · every ${monitor.intervalSeconds}s` : `${monitor.monitorType.toUpperCase()} · ${monitor.url}`}</span>}<div className="card-data"><div><span>{monitor.monitorType === 'http' ? 'Response' : monitor.monitorType === 'heartbeat' ? 'Last ping' : 'Latency'}</span><strong>{monitor.monitorType === 'heartbeat' ? date(monitor.lastCheckedAt) : monitor.lastResponseMs == null ? '—' : `${monitor.lastResponseMs} ms`}</strong></div><div><span>Last check</span><strong>{date(monitor.lastCheckedAt)}</strong></div><div><span>Alert policy</span><strong>{monitor.notificationChannels.join(', ') || 'Muted'} · {monitor.escalationDelaySeconds}s delay</strong></div></div><label className="check push-choice"><input type="checkbox" checked={pushMonitorIds.includes(monitor.id)} onChange={(e) => setPushMonitorIds(e.target.checked ? [...pushMonitorIds, monitor.id] : pushMonitorIds.filter((id) => id !== monitor.id))} /> Browser alerts for this monitor</label>{monitor.expectedKeyword && <p className="monitor-kind">Content contains “{monitor.expectedKeyword}”</p>}{monitor.lastError && <p className="incident">{monitor.lastError}</p>}<div className="card-actions"><button className="secondary" onClick={() => edit(monitor)}>Edit</button><button className="danger" onClick={() => remove(monitor)}>Delete</button></div><div className="schedule-box"><small>Weekly maintenance windows</small>{(schedules[monitor.id] || []).map((schedule) => <div className="schedule-row" key={schedule.id}><span>{weekdays[schedule.weekday]} {scheduleTime(schedule.startMinute)} · {schedule.durationMinutes}m · {schedule.timezone}{schedule.exceptionDates.length ? ` · skips ${schedule.exceptionDates.join(', ')}` : ''}</span><button className="danger compact" onClick={() => removeSchedule(monitor.id, schedule.id)}>Remove</button></div>)}<div className="schedule-form"><select value={scheduleDraft.weekday} onChange={(e) => setScheduleDraft({ ...scheduleDraft, weekday: Number(e.target.value) })}>{weekdays.map((day, index) => <option value={index} key={day}>{day}</option>)}</select><input type="time" value={scheduleTime(scheduleDraft.startMinute)} onChange={(e) => { const [hours, minutes] = e.target.value.split(':').map(Number); setScheduleDraft({ ...scheduleDraft, startMinute: hours * 60 + minutes }) }} /><input type="number" min={1} max={1440 - scheduleDraft.startMinute} value={scheduleDraft.durationMinutes} onChange={(e) => setScheduleDraft({ ...scheduleDraft, durationMinutes: Number(e.target.value) })} /><input value={scheduleDraft.timezone} maxLength={64} onChange={(e) => setScheduleDraft({ ...scheduleDraft, timezone: e.target.value })} aria-label="Timezone" /><input value={scheduleDraft.exceptionDates} onChange={(e) => setScheduleDraft({ ...scheduleDraft, exceptionDates: e.target.value })} aria-label="Exception dates" placeholder="2026-12-28, 2027-01-04" /><button className="secondary" onClick={() => addSchedule(monitor.id)}>Add window</button></div></div></article>)}</div>
        {monitors.length > 0 && <button className="secondary push-save" onClick={configurePush}>Save browser alert selection</button>}
      </section>
      <section className="incident-section"><div className="section-title"><div><p className="kicker">TIMELINE</p><h2>Recent incidents</h2></div></div><div className="timeline">{incidents.length === 0 ? <div className="empty">No incidents recorded.</div> : incidents.map((incident) => <article className="timeline-row" key={incident.id}><i className={incident.resolvedAt ? 'resolved' : 'open'} /><div><div className="timeline-title"><strong>{incident.monitorName}</strong><span className={`pill ${incident.resolvedAt ? 'up' : 'down'}`}>{incident.resolvedAt ? 'resolved' : incident.acknowledgedAt ? 'acknowledged' : 'open'}</span></div><p>{incident.cause}{incident.note && ` · ${incident.note}`}</p><small>Started {date(incident.startedAt)}{incident.resolvedAt && ` · Resolved ${date(incident.resolvedAt)}`}</small><div><button className="secondary compact" onClick={() => annotate(incident)}>Internal note</button> <button className="secondary compact" onClick={() => publishUpdate(incident)}>Publish update</button></div></div></article>)}</div></section>
      {audit.length > 0 && <section className="panel audit"><div className="section-title"><div><p className="kicker">ACCOUNTABILITY</p><h2>Recent changes</h2></div></div><div className="audit-list">{audit.slice(0, 20).map((event, index) => <div className="audit-row" key={`${event.createdAt}-${index}`}><span className="audit-action">{event.action}</span><span className="audit-resource">{event.resource}</span><span className="audit-actor">{event.actor}</span><time dateTime={event.createdAt}>{date(event.createdAt)}</time></div>)}</div></section>}
      <section className="panel keys"><div><p className="kicker">TEAM ACCESS</p><h2>Members and invitations</h2><p>Each accepted invitation creates independently revocable access.</p></div><form onSubmit={createInvitation}><label>Invitee<input value={newInvitation.name} maxLength={100} onChange={(e) => setNewInvitation({ ...newInvitation, name: e.target.value })} placeholder="On-call engineer" required /></label><label>Role<select value={newInvitation.scope} onChange={(e) => setNewInvitation({ ...newInvitation, scope: e.target.value as Invitation['scope'] })}><option value="read">Viewer</option><option value="write">Operator</option><option value="admin">Admin</option></select></label><label>Expires in hours<input type="number" min={1} max={720} value={newInvitation.expiresInHours} onChange={(e) => setNewInvitation({ ...newInvitation, expiresInHours: Number(e.target.value) })} /></label><button>Create invitation</button></form>{invitationLink && <p className="token-result"><strong>Copy this expiring link:</strong> <code>{invitationLink}</code></p>}<div className="key-list">{invitations.map((invite) => <div key={invite.id}><span><strong>{invite.name}</strong><small>{invite.scope} · expires {date(invite.expiresAt)} · {invite.acceptedAt ? 'accepted' : invite.revokedAt ? 'revoked' : 'pending'}</small></span>{!invite.acceptedAt && !invite.revokedAt && <button className="danger compact" onClick={() => revokeInvitation(invite)}>Revoke invitation</button>}</div>)}</div><hr /><form onSubmit={createKey}><label>Direct key name<input value={newKey.name} maxLength={100} onChange={(e) => setNewKey({ ...newKey, name: e.target.value })} placeholder="Deployment integration" required /></label><label>Role<select value={newKey.scope} onChange={(e) => setNewKey({ ...newKey, scope: e.target.value as 'read' | 'write' | 'admin' })}><option value="read">Viewer</option><option value="write">Operator</option><option value="admin">Admin</option></select></label><button>Create access key</button></form>{createdToken && <p className="token-result"><strong>Copy now — shown once:</strong> <code>{createdToken}</code></p>}<div className="key-list">{keys.map((key) => <div key={key.id}><span><strong>{key.name}</strong><small>{key.prefix}… · {key.scope === 'read' ? 'viewer' : key.scope === 'write' ? 'operator' : 'admin'} · {key.lastUsedAt ? `used ${date(key.lastUsedAt)}` : 'never used'}</small></span><button className="danger compact" onClick={() => deleteKey(key)}>Revoke</button></div>)}</div></section>
    </main>
    <footer>PulseOps · PostgreSQL-backed monitoring</footer>
  </div>
}
